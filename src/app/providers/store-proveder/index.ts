import StoreProvider from './ui/StoreProveder';
import { store, createReduxStore, RootState, AppDispatch } from './config/store';


export
{
  StoreProvider,
  createReduxStore,
  store,
  RootState,
  AppDispatch,
};